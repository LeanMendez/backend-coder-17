// import {calcRandomNumbers} from './random.js'

// //flag para saber que hay conexion de padre a hijo
// process.send("listo")

// process.on("message", (parent) => {
//   const number = calcRandomNumbers(parent);
  
//   process.send(number);
// });
